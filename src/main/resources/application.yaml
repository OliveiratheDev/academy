server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gym
    username: dev
    password: dev123

  jpa:
    hibernate:
      ddl-auto: update

  docker:
    compose:
      enabled: false
